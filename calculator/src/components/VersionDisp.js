import React from 'react';

const VersionDisp = ({versionNo}) => {
    return <div>
        <span>Version</span>{' '}<span><strong>{versionNo}</strong></span>
    </div>
}

export default VersionDisp;